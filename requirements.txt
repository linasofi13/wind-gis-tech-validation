# Vento Wind GIS Technology Validation - Python Requirements
# Core dependencies for wind suitability analysis

# Core scientific computing
numpy>=1.21.0
scipy>=1.7.0
pandas>=1.3.0
geopandas>=0.10.0

# Geospatial libraries
rasterio>=1.3.0
rioxarray>=0.9.0
shapely>=1.8.0
fiona>=1.8.0
pyproj>=3.3.0

# Visualization
folium>=0.12.0
matplotlib>=3.5.0
seaborn>=0.11.0

# CLI and utilities
typer[all]>=0.6.0
pyyaml>=6.0
psutil>=5.8.0
jinja2>=3.0.0
reportlab>=3.6.0

# Development and testing
pytest>=6.0.0
pytest-cov>=3.0.0
black>=22.0.0
flake8>=4.0.0
mypy>=0.950

# Optional: Advanced visualization (compatible with Python 3.8)
# kepler-gl>=0.3.0  # Not available for Python 3.8
pydeck>=0.8.0
streamlit>=1.0.0

# Optional: QGIS integration (requires QGIS installation)
# qgis==4.20.0  # Uncomment if QGIS is available
PyQt5>=5.15.0  # Required for PyQGIS integration

# Optional: ArcGIS integration (requires ArcGIS installation)
# arcpy  # Uncomment if ArcGIS is available